# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    client:
        label: Cliente
        relation: client
        select: name
        searchable: true
    title:
        label: Título
        searchable: true
    user:
        label: Usuario
        relation: user
        select: email
        searchable: true