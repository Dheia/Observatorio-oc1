# ===================================
#  Form Field Definitions
# ===================================

fields:
    id:
        label: ID
        disabled: true
    client:
        label: Cliente
        type: relation
    shop:
        label: Tienda
        type: relation